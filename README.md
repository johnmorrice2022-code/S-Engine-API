# S-Engine-Physics-API

An API for UK Physics teachers that:

produces genuinely helpful PowerPoint slides

uses LaTeX when needed

inserts curriculum-aligned diagrams/images

is driven by dropdowns + a teacher prompt

has a simple, efficient UI

is marketable
